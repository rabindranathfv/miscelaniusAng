import { TitleDecoratorDirective } from './title-decorator.directive';

describe('TitleDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
